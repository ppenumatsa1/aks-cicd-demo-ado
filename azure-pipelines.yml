# Deploy to Azure Kubernetes Service
# Build and push image to Azure Container Registry; Deploy to Azure Kubernetes Service


trigger:
- main


resources:
- repo: self

variables:
  #Repo Named
  repoName: 'myapp'  

  # ACR Registry ADO Service Connections
  containerRegistryDev: 'sc-acrdev341270475'
  containerRegistryStage: 'sc-acrstage341270475'
  containerRegistryProd: 'sc-acrprod341270475'
  dockerfilePath: '**/Dockerfile'

  acrdevurl: 'acrdev341270475.azurecr.io'
  acrstageurl: 'acrstage341270475.azurecr.io'
  acrprodurl: 'acrprod341270475.azurecr.io'

  imagePullSecretDev: 'acrdev341270475-auth'
  imagePullSecretStage: 'acrstage341270475-auth'
  imagePullSecretProd: 'acrprod341270475-auth'

  #kubernetes ADO Environments
  envdev: 'env-aks-dev-341270475.default'
  envstage: 'env-aks-staging-341270475.default'
  envprod: 'env-aks-prod-341270475.default'
 
  tag: '$(Build.BuildId)'

  # Agent VM image name
  vmImageName: 'ubuntu-latest'


stages:
- stage: CI
  displayName: Build stage
  jobs:
  - job: Build
    displayName: Build
    pool: 
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build Container Image
      inputs:
        command: build
        repository: $(repoName)
        dockerfile: $(dockerfilePath)
        tags: $(tag)

    - task: Docker@2
      displayName: Save Image in CI
      inputs:
        repository: $(repoName)
        command: save
        arguments: '--output $(Pipeline.Workspace)/$(repoName).image.tar $(repoName):$(tag)'
        addPipelineData: false


    - task: PublishPipelineArtifact@1
      displayName: Publish Image Artifact
      inputs:
        targetPath: '$(Pipeline.Workspace)'
        artifact: 'ContainerImage'
        publishLocation: 'pipeline'
      


- stage: CDDEV
  displayName: CD for Dev Env
  dependsOn: CI

  jobs:
  - deployment: Deploy
    displayName: Deploy to Dev Environment
    variables:
    - name: acr-url
      value: $(acrdevurl)
    - name: image-name
      value: $(repoName)
    pool: 
      vmImage: $(vmImageName)
    environment: $(envdev)
    strategy:
      runOnce:
        deploy:
          steps:
          - task: Docker@2
            displayName: Load Image from CI
            inputs:
              command: load
              arguments: '--input $(Pipeline.Workspace)/ContainerImage/$(repoName).image.tar'
            
          - task: Docker@2
            displayName: ReTag Image with ACR Name - BuildId
            inputs:
              containerRegistry: '$(containerRegistryDev)'
              repository: '$(repoName)'
              command: tag
              arguments: '$(repoName):$(tag)  $(acrdevurl)/$(repoName):$(tag)'


          - task: Docker@2
            displayName: Push Image to Dev ACR
            inputs:
              containerRegistry: $(containerRegistryDev)
              repository: $(repoName)
              command: push
              tags: $(tag)
          

          - task: KubernetesManifest@0
            inputs:
              action: createSecret
              namespace: default
              secretType: dockerRegistry
              secretName: $(imagePullSecretDev)
              dockerRegistryEndpoint: $(containerRegistryDev)

          - task: replacetokens@5
            inputs:
              rootDirectory: '$(Pipeline.Workspace)/ContainerImage/s/manifests'
              targetFiles: 'deployment.yml'
              tokenPattern: 'custom'
              writeBOM: true
              actionOnMissing: 'warn'
              keepToken: false
              tokenPrefix: __
              tokenSuffix: __
              actionOnNoFiles: 'continue'
              enableTelemetry: true

          - task: KubernetesManifest@0
            displayName: Deploy to Kubernetes cluster
            inputs:
              action: deploy
              manifests: |
                $(Pipeline.Workspace)/ContainerImage/s/manifests/deployment.yml
                $(Pipeline.Workspace)/ContainerImage/s/manifests/service.yml
              imagePullSecrets: 
                $(imagePullSecretDev)
              containers: |
                $(acrdevurl)/$(repoName):$(tag)
            



- stage: CDStage
  displayName: CD for Stage Env
  

  jobs:
  - deployment: DeployStage
    displayName: Deploy to Stage Environment
    variables:
    - name: acr-url
      value: $(acrstageurl)
    - name: image-name
      value: $(repoName)
    pool: 
      vmImage: $(vmImageName)
    environment: $(envstage)
    strategy:
      runOnce:
        deploy:
          steps:
          - task: Docker@2
            displayName: Load Image from CI
            inputs:
              command: load
              arguments: '--input $(Pipeline.Workspace)/ContainerImage/$(repoName).image.tar'
            
          - task: Docker@2
            displayName: ReTag Image with ACR Name - BuildId
            inputs:
              containerRegistry: $(containerRegistryStage)
              repository: $(repoName)
              command: tag
              arguments: '$(repoName):$(tag)  $(acrstageurl)/$(repoName):$(tag)'


          - task: Docker@2
            displayName: Push Image to Stage ACR
            inputs:
              containerRegistry: $(containerRegistryStage)
              repository: $(repoName)
              command: push
              tags: $(tag)
            
          - task: replacetokens@5
            inputs:
              rootDirectory: '$(Pipeline.Workspace)/ContainerImage/s/manifests'
              targetFiles: 'deployment.yml'
              tokenPattern: 'custom'
              writeBOM: true
              actionOnMissing: 'warn'
              keepToken: false
              tokenPrefix: __
              tokenSuffix: __
              actionOnNoFiles: 'continue'
              enableTelemetry: true
            
          - task: KubernetesManifest@0
            inputs:
              action: 'createSecret'
              namespace: 'default'
              secretType: 'dockerRegistry'
              secretName: $(imagePullSecretStage)
              dockerRegistryEndpoint: $(containerRegistryStage)
          
          - task: KubernetesManifest@0
            displayName: Deploy to Kubernetes cluster
            inputs:
              action: deploy
              manifests: |
                $(Pipeline.Workspace)/ContainerImage/s/manifests/deployment.yml
                $(Pipeline.Workspace)/ContainerImage/s/manifests/service.yml
              imagePullSecrets: |
                $(imagePullSecretStage)
              containers: |
                $(acrstageurl)/$(repoName):$(tag)
              

- stage: CDProd
  displayName: CD for Prod Env
  

  jobs:
  - deployment: DeployProd
    displayName: Deploy to Prod Environment
    variables:
    - name: acr-url
      value: $(acrprodurl)
    - name: image-name
      value: $(repoName)
    pool: 
      vmImage: $(vmImageName)
    environment: $(envprod)
    strategy:
      runOnce:
        deploy:
          steps:
          - task: Docker@2
            displayName: Load Image from CI
            inputs:
              command: load
              arguments: '--input $(Pipeline.Workspace)/ContainerImage/$(repoName).image.tar'
            
          - task: Docker@2
            displayName: ReTag Image with ACR Name - BuildId
            inputs:
              containerRegistry: $(containerRegistryProd)
              repository: $(repoName)
              command: tag
              arguments: '$(repoName):$(tag)  $(acrprodurl)/$(repoName):$(tag)'


          - task: Docker@2
            displayName: Push Image to Stage ACR
            inputs:
              containerRegistry: $(containerRegistryProd)
              repository: $(repoName)
              command: push
              tags: $(tag)
          
          - task: KubernetesManifest@0
            inputs:
              action: 'createSecret'
              namespace: 'default'
              secretType: 'dockerRegistry'
              secretName: $(imagePullSecretProd)
              dockerRegistryEndpoint: $(containerRegistryProd)

          - task: replacetokens@5
            inputs:
              rootDirectory: '$(Pipeline.Workspace)/ContainerImage/s/manifests'
              targetFiles: 'deployment.yml'
              tokenPattern: 'custom'
              writeBOM: true
              actionOnMissing: 'warn'
              keepToken: false
              tokenPrefix: __
              tokenSuffix: __
              actionOnNoFiles: 'continue'
              enableTelemetry: true


          - task: KubernetesManifest@0
            displayName: Deploy to Kubernetes cluster
            inputs:
              action: deploy
              manifests: |
                $(Pipeline.Workspace)/ContainerImage/s/manifests/deployment.yml
                $(Pipeline.Workspace)/ContainerImage/s/manifests/service.yml
              imagePullSecrets: |
                $(imagePullSecretProd)
              containers: |
                $(acrprodurl)/$(repoName):$(tag)
